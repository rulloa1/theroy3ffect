# Development placeholders — overridden by /run/base44/app.env (platform secrets).
# These are NOT real credentials; they only unblock startup. Replace via the
# Base44 Secrets dashboard for full functionality.

# Supabase service role key (server-side admin operations)
SUPABASE_SERVICE_ROLE_KEY=sb_secret_placeholder

# Stripe sandbox API key (server-side payment processing)
STRIPE_SANDBOX_API_KEY=sk_test_placeholder
STRIPE_LIVE_API_KEY=

# Lovable API key (Stripe gateway connector)
LOVABLE_API_KEY=lovable_placeholder

# Vapi server secret (webhook verification)
VAPI_SERVER_SECRET=vapi_placeholder

# GoHighLevel inbound webhook (optional — form lead sync)
GHL_INBOUND_WEBHOOK_URL=

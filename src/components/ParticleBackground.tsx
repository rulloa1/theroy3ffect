/** Lightweight static background retained for secondary public routes. */
export function ParticleBackground() {
  return <div className="particle-background pointer-events-none fixed inset-0" aria-hidden="true" />;
}